let number = 1;

if(number == "1")
    console.log("Double Equal");  // this one will execute only
if(number === "1") // it won't because it strictly checks datatype too
    console.log("Triple Equal"); 